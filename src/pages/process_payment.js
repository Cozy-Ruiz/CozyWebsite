/*
import { MercadoPagoConfig, Payment } from '@mercadopago/sdk-react';

const client = new MercadoPagoConfig({ accessToken: 'TEST-3618815041375960-052817-1b36681ba31bfb1696e5836a0628008a-152028312' });

const payment = new Payment(client);
payment.create({ body: req.body })
.then(console.log)
.catch(console.log);
*/
import React from "react";

const ProcessPayment = () => {
    return (
      <div>
        {/* Contenido del componente */}
      </div>
    );
  };
  
  export default ProcessPayment;